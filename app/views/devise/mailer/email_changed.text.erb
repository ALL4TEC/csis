<%= t('devise.mailer.email_changed.greeting', recipient: User.find_by(email: @email).full_name) %>
===============================================

<% if @resource.try(:unconfirmed_email?) %>
  <%= t('devise.mailer.email_change.message', email: @resource.unconfirmed_email) %>
<% else %>
  <%= t('devise.mailer.email_changed.message', email: @resource.email) %>
<% end %>
<%= I18n.t('mailer.content.polite') %>

<%= I18n.t('mailer.content.do_not_answer') %>
